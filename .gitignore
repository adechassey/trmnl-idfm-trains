# trmnlp build output
_build/

# Never commit the PRIM API key: keep it in .env (loaded by bin/dev).
# .trmnlp.yml is committed by TRMNL's GitHub sync with empty custom fields; keep your
# local values out of git with: git update-index --skip-worktree .trmnlp.yml
.env

# smoke test renders
/dev/.smoke/
/dev/fixture/_build/
__pycache__/
